// if( $('#<%= dom_id(@task) %> a').eq(2).html() === 'Done' ) {
//   $('#<%= dom_id(@task) %> a').eq(2).html('Not Done').addClass('btn btn-warning btn-xs'); 
// }
// else if ( $('#<%= dom_id(@task) %> a').eq(2).html() === 'Not Done' ){
//   $('#<%= dom_id(@task) %> a').eq(2).html('Done').removeClass('btn btn-warning btn-xs').addClass('btn btn-success btn-xs'); 
// }


if( $('#<%= dom_id(@task) %> a').eq(2).html() === 'Done' ) {
  $('#<%= dom_id(@task) %> a').eq(2).html('Not Done').addClass('btn btn-warning btn-xs'); 
}
else {
  $('#<%= dom_id(@task) %> a').eq(2).html('Done').removeClass('btn btn-warning btn-xs').addClass('btn btn-success btn-xs'); 
}